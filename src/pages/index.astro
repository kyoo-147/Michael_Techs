---
import HomePage from "@/components/pages/HomePage.astro";
import { getLocalizedFeaturedWork, getLocalizedRecentPosts } from "@/utils/content";

const locale = "en";
const posts = await getLocalizedRecentPosts(locale, 4);
const work = await getLocalizedFeaturedWork(locale, 5);
---

<HomePage locale={locale} {posts} {work} />
